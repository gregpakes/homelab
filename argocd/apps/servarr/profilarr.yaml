apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: profilarr
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: servarr
  sources:

    - repoURL: https://bjw-s-labs.github.io/helm-charts/
      chart: app-template
      targetRevision: 4.5.0
      helm:
        valueFiles:
          - $values/argocd/infrastructure/servarr/profilarr/values.yaml

    - repoURL: https://github.com/gregpakes/homelab
      targetRevision: main
      ref: values
      path: argocd/infrastructure/servarr/profilarr
      directory:
        recurse: true  

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true