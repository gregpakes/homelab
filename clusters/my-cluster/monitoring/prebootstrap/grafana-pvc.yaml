apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rancher-monitoring-grafana
  namespace: cattle-monitoring-system
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn   # keep this explicit so it won't use local-path